This folder contains non-PowerShell helpers preserved for reference.

- scripts/*.sh moved here from the root scripts directory (Linux/macOS Bash versions).
- These are not maintained for this repo (PowerShell-only).

Use the PowerShell scripts in the root scripts/ directory for supported workflows on Windows.
